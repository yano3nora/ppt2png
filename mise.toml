# https://mise.jdx.dev/
# Project-local tool versions and tasks.
# Uncomment only the parts this project actually needs.

# [settings]
# experimental = true # required by [shell_alias]

# [tools]
# node = "24.14.1"
# jq = "1.7.1"
# ripgrep = "15.1.0"

# [env]
# _.file = ".env.local"

# [shell_alias]
# provision = "mise run provision"

# [tasks.pre-commit]
# description = "pre-commit: fix lint, format and run tests"
# run = [
#   "npm run fix",
#   "npm test",
# ]

# [tasks.provision]
# description = "provisioning"
# run = [
#   "mise generate git-pre-commit --task pre-commit --write",
#   "npm ci",
# ]
